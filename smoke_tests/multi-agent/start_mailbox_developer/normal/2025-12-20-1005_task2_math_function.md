Date: 2025-12-20T10:05:00Z
From: smoke-test-mgr_manager
To: smoke-test-dev_developer
Subject: Task 2 - Add Math Function
Priority: NORMAL
MessageType: unstructured
---

Create a math utility module.

## Requirements

- Create file `math-utils.js`
- Add function `add(a, b)` that returns the sum of `a` and `b`
- Add function `multiply(a, b)` that returns the product of `a` and `b`
- Export both functions using `module.exports = { add, multiply }`

## Acceptance Criteria

- `math-utils.js` exists
- `add(2, 3)` returns `5`
- `multiply(4, 5)` returns `20`
- Both functions are exported
